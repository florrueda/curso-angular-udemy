const name = "strider";

let hpPoints: number | "full" = 95;

// number | string --> forma de poner que una variable va a ser de un tipo de dato u otro

const isalive: boolean = true;

console.log({ name, hpPoints, isalive});


export {};